# The dsh-tianshu-tui bundle patch: mounts the interactive terminal UI over
# dsh-base and moves the agent plane behind official agent presets (same split
# as dsh-web-app). Host keeps registries, sandbox, approval, and model routing.
# The runner hosts the ported render core and TuiApp (SOURCE-MAP.md / README.md).

- insert:
    - id: tui-runner
      name: '@huiliyi37/dsh-tianshu-tui'

    - id: agent-presets
      name: '@deepseek-ai/dsh-agent-presets'
      config:
        default: standard

    # rc.1 起 standard preset 的 tool-subagent 行开 modelSelectionSettings: true，
    # 要求 Host scope 有 model-selection-settings 服务（官方 web-app 有、dsh-base
    # 无）——不挂则 standard 挂载整体失败，会话无工具面。shipped 组成默认关。
    - id: subagent-model-selection-settings
      name: '@deepseek-ai/dsh-tool-subagent/model-selection-settings'

    # LSP 模型工具面（任务6对齐，2026-08-27）：tianshu-public 0.6.0 三件套——
    # seam（ctx.lsp 注册表）+ stdio 本地 provider + 模型工具（单 lsp 工具四操作：
    # goToDefinition / findReferences / goToImplementation / hover）。lsp-local
    # 默认给 typescript-language-server 一个 npx 条目（懒启动；npx 随 node 在场，
    # 加载期解析不炸，首次查询按需拉取）；其他语言由用户照此追加 servers 条目。
    # 无 provider 时模型查询收到结构化 LSP_UNAVAILABLE（schema 稳定）。
    - id: lsp
      name: '@huiliyi37/dsh-lsp'

    - id: lsp-local
      name: '@huiliyi37/dsh-lsp-local'
      config:
        servers:
          typescript:
            command: npx
            args: ['-y', 'typescript-language-server', '--stdio']
            extensionToLanguage:
              .ts: typescript
              .tsx: typescript
              .js: javascript
              .jsx: javascript
              .mjs: javascript
              .cjs: javascript

    - id: tool-lsp
      name: '@huiliyi37/dsh-tool-lsp'

# ── agent plane leaves the host (standard preset remounts these) ───────────

- id: tool-bash
  disabled: true

- id: tool-pwsh
  disabled: true

- id: tool-jobs
  disabled: true

- id: tool-fs
  disabled: true

- id: tool-fs-search
  disabled: true

- id: tool-str-replace-editor
  disabled: true

- id: skill-filesystem
  disabled: true

- id: tool-skill
  disabled: true

- id: tool-goal
  disabled: true

- id: plan-mode
  disabled: true

- id: compaction-basic
  disabled: true

- id: command-compact
  disabled: true

- id: tool-result-pruner
  disabled: true

- id: tool-subagent-control
  disabled: true

- id: tool-subagent-list-agents
  disabled: true

- id: tool-subagent
  disabled: true

- id: tool-subagent-fork
  disabled: true

- id: workflow-worker-thread
  disabled: true

- id: tool-workflow
  disabled: true

- id: tool-ralph
  disabled: true

- id: agent-instructions
  disabled: true

- id: tool-todo
  disabled: true

- id: tool-web
  disabled: true
